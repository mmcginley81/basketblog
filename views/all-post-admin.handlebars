
{{#each post}}
<article class="post">
  <div class="title">
    <a href="{{post_text}}" target="_blank">{{title}}</a>
  </div>
  <div class="meta">
    by {{user.username}} on 
    |
    <a href="/post/{{id}}">{{comments.length}}</a>
  </div>
</article>
{{/each}}